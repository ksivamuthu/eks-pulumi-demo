apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "coffee-api-helm.fullname" . }}-test-connection"
  labels:
{{ include "coffee-api-helm.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "coffee-api-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
