apiVersion: v1
kind: Service
metadata:
  name: {{ include "coffee-api-helm.fullname" . }}
  labels:
{{ include "coffee-api-helm.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "coffee-api-helm.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
